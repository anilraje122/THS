import React from "react";
import ReactDOM from "react-dom";
import App from "./App";

ReactDOM.render(<App />, document.getElementById("root"));

/* 

1. React Styling

2. In React components can be written in two ways:
  a. Class component
     Statefull component should be written using class component
  b. Functional component
     Stateless component should be written using functions component
     Functional component with state is called as Hooks (Latest)

3. Props and PropTypes

4. State : 
   a. getState
   b. setState

5. Looping Components

6. <App /> = React.createElement('App') 
   
   Babel helps to convert it

7. Component Lifecycle : Things before rendering and after rendering
   Eg: componentDidMount

*/
